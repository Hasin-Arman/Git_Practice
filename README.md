How to push into the github from the local machine?
Ans : 
    1) Intialize the git using git init
    2) Add the specific file or the whole file in the folder using git add . or git add README.md
    3) Then Write a commit message using git commit -m "message"
    4) Then push into a branch using git push origin/main
